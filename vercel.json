{
  "framework": "nextjs",
  "installCommand": "pnpm install --no-frozen-lockfile --registry https://registry.npmjs.org",
  "buildCommand": "pnpm run build --filter=web",
  "outputDirectory": "apps/web/.next",
  "devCommand": "pnpm run dev --filter=web"
}