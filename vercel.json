{
  "framework": "nextjs",
  "installCommand": "pnpm install --no-frozen-lockfile --registry https://registry.npmmirror.com",
  "buildCommand": "pnpm run build --filter=web",
  "outputDirectory": "apps/web/.next",
  "devCommand": "pnpm run dev --filter=web"
}