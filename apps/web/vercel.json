{
  "framework": "nextjs",
  "installCommand": "corepack enable pnpm && corepack use pnpm@8.15.6 && cd ../.. && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm run build --filter=web",
  "outputDirectory": ".next",
  "devCommand": "pnpm run dev",
  "crons": [
    {
      "path": "/api/cron/process-emails",
      "schedule": "0 9 * * *"
    }
  ]
}
