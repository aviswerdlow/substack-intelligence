// Mock for server-only module used in tests
// This module is used by Next.js to prevent server code from being imported in client components
// In tests, we can safely mock it as an empty module
export default {};